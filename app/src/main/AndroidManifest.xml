<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.findMyClassmates">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <!-- LoginActivity is the main launching activity for the app -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".InboxActivity" />
        <activity android:name=".FindActivity" />
        <activity android:name=".MessageActivity" />

        <activity android:name=".FeedbackActivity" />
        <activity android:name=".WriteFeedbackActivity" />
        <activity android:name=".EditFeedbackActivity" />
        <activity android:name=".ClassmatesProfile" />


        <!-- MainActivity is another activity in the app -->
        <activity android:name=".MainActivity"></activity>


    </application>

</manifest>
